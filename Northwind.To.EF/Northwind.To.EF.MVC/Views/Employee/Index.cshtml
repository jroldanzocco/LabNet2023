@model List<Northwind.To.EF.MVC.Models.EmployeeView>

@{
    ViewBag.Title = "Employee Page";
}
<h1>Pagina de empleados - Index</h1>

<div class="row" style="text-align:right;padding:5px">
    @Html.ActionLink("Agregar", "Insert","Employee", new { @class = "btn btn-primary"})
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">APELLIDO</th>
            <th scope="col">NOMBRE</th>
            <th scope="col">ROL</th>
            <th scope="col">Accion</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var empleado in Model)
        {
        <tr>
            <th scope="row">@empleado.Id</th>
            <td>@empleado.LastName</td>
            <td>@empleado.FirstName</td>
            <td>@empleado.Title</td>
            <td>@Html.ActionLink("Eliminar","Delete", new { id = empleado.Id}, new {onclick = "return confirm('Esta seguro de borrar?')"})</td>
            
        </tr>
        }
        </tbody>
</table>

<div>
    @Html.ActionLink("Agregar empleado", "Insert")
</div>
